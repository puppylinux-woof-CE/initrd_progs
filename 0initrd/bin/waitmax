#!/bin/ash

if [ "$1" = "" -o "$2" = "" ] ; then
	echo "
Usage: waitmax MAXTIME PROGRAM [ARGS...]

Execute PROGRAM as a subprocess. If PROGRAM does not exit before MAXTIME
seconds, it will be killed with SIGTERM.
"
	exit 1
fi

busybox timeout -t $@ 2>/dev/null
exit 255

